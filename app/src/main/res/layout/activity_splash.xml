<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/background_image"
    android:gravity="center"
    tools:context="com.example.capsule.SplashActivity">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/capsule_logo"
        android:layout_centerHorizontal="true"
        android:contentDescription="LOGO"
        tools:ignore="HardcodedText" />

    <RelativeLayout
        android:id="@+id/relativeTxtCapsulePharmacy"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/logo"
        android:gravity="center"
        android:layout_marginTop="30dp">

        <TextView
            android:id="@+id/txtCapsule"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/archivo_black"
            android:text="capsule"
            android:textAlignment="center"
            android:textAllCaps="true"
            android:textColor="@color/black"
            android:textSize="40sp"
            tools:ignore="HardcodedText" />

        <TextView
            android:id="@+id/txtPharmacy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/txtCapsule"
            android:layout_marginStart="10dp"
            android:layout_marginBottom="5dp"
            android:layout_toEndOf="@+id/txtCapsule"
            android:fontFamily="sans-serif-condensed-medium"
            android:text="Pharmacy"
            android:textAlignment="center"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:textStyle="italic"
            tools:ignore="HardcodedText" />

    </RelativeLayout>

    <ProgressBar
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/relativeTxtCapsulePharmacy"
        android:layout_marginTop="40dp"
        android:indeterminateTint="#2788FF"/>

</RelativeLayout>